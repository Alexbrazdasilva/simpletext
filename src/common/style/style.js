import colors from "vuetify/lib/util/colors";
const { orange, grey } = colors;

const themes = {
  light: {
    primary: orange,
  },
  dark: {
    primary: grey.darken4,
  },
};

export default themes;
